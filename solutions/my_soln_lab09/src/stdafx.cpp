#include <stdafx.h>

// Intentionally empty!  This file is necessary because we set a File (not Project) property
// on it that says to *build* the pre-compiled header file (all the other files are set to
// *use* it [they inherit this property from the project]).